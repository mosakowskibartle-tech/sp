/**
 * supabase.ts — stub file.
 * This project now uses Railway PostgreSQL via /api/* routes.
 * This file exists only for backward-compat imports.
 */
export const supabase = null;
export default supabase;
